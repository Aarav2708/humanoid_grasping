name: zeroshot_env
channels:
  - pytorch
  - conda-forge
  - defaults

dependencies:
  - python=3.9
  - pip
  - numpy
  - opencv
  - matplotlib
  - pytorch
  - torchvision
  - torchaudio
  - cudatoolkit=11.8  # Match your CUDA version; or remove if using CPU
  - pip:
      - pyrealsense2
      - segment-anything
